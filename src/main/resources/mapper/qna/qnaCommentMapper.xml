<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.trycatch.mapper.qna.QnaCommentMapper">
    <insert id="insert">
        insert into tbl_qna_comment(id, corp_id, qna_comment_parent, qna_comment_content, qna_comment_like_count)
        values (#{id},#{corpId},#{qnaCommentParent},#{qnaCommentContent},#{qnaCommentLikeCount})
    </insert>

    <select id="selectByQnaId" resultType="QnaCommentDTO">
        select * from tbl_qna_comment where id = #{qnaId} order by created_datetime asc
    </select>

    <delete id="delete">
        delete from tbl_qna_comment where id = #{id}
    </delete>

</mapper>